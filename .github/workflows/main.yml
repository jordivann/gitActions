name: Test

on: [push]

jobs: 
  test-build: 
    runs-on: ubuntu-latest
    
    steps: 
      - name: Checkout
        uses: actions/checkout@v4.2.1  
      - name: ls
        ##con pipe | puedo hacer multilinea
        run: | 
          chmod +x script.sh
          ./script.sh

      - name: ls
        users: ./.github/actions/my-action